---
import Layout from "../components/Layout.astro";
---

<Layout>
  <div class="input-area">
    <h2>どんなカーペットを探していますか？</h2>
    <p>色の強度やイメージを入力してください</p>
    <input type="text" placeholder="色を選択" />
    <button type="button">生成する →</button>
  </div>

  <style>
    .input-area {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 50px;
      background-color: #FFF; /* 背景色白 */
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      margin: 100px auto; /* ページ中央に配置 */
      width: 80%; /* 幅を適切に設定 */
      max-width: 600px; /* 最大幅を設定 */
    }

    h2 {
      color: #333;
      font-size: 1.5rem;
      text-align: center;
      margin-bottom: 20px;
    }

    p {
      text-align: center;
      margin-bottom: 30px;
      color: #666;
    }

    input[type="text"] {
      width: 100%;
      padding: 15px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box; /* 入力フィールドの内側にパディングを含める */
    }

    button {
      padding: 15px 30px;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #0056b3;
    }
  </style>
</Layout>
